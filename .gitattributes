# Force LF line endings for shell scripts (prevents CRLF issues in Docker containers)
*.sh text eol=lf
docker/*.sh text eol=lf

# Keep Windows line endings for batch/powershell
*.bat text eol=crlf
*.ps1 text eol=crlf
*.cmd text eol=crlf
